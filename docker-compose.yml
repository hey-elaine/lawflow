services:
  lawflow:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      - LAWFLOW_MCP_TOKEN=${LAWFLOW_MCP_TOKEN:-}
    restart: unless-stopped